<Window x:Class="eSoftDesktop.Forms.DealForm"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:eSoftDesktop.Forms"
        mc:Ignorable="d"
        Title="DealForm" Height="450" Width="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="36*" />
            <RowDefinition Height="339*" />
            <RowDefinition Height="44*" />
        </Grid.RowDefinitions>
        <TextBlock Text="Управление сделками"/>
        <DataGrid x:Name="dgDeal" AutoGenerateColumns="False" Grid.Row="1">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Id" Binding="{Binding IdDeal}"/>
                <DataGridTextColumn Header="Предложение" Binding="{Binding IdDemand}"/>
                <DataGridTextColumn Header="Спрос" Binding="{Binding IdSupply}"/>
            </DataGrid.Columns>
        </DataGrid>
        <Button Content="Add" Click="clAdd" HorizontalAlignment="Left" Grid.Row="2"/>
        <Button Content="Delete" Click="clDelete" HorizontalAlignment="Right" Grid.Row="2"/>
        <Button Content="Edit" Click="clEdit" HorizontalAlignment="Center" Grid.Row="2" />
    </Grid>
</Window>
